#pragma once

namespace rz
{

struct ISubSystem;

struct ISubSystemManager
{
	///
	//virtual void Register(const ISubSystem &aSubSystem) = 0;
	
	///
	//virtual void Unregister(const ISubSystem &aSubSystem) = 0;
	
	///
	//virtual ISubSystem *GetByName(const char *asName) const = 0;
	
	///
	//template<typename T>
	//inline T *GetSubSystem() const {return GetSubSystem(T);}
};

}; // namespace rz